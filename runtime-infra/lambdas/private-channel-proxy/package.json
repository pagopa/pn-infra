{
  "name": "private-channel-proxy",
  "version": "1.0.0",
  "description": "AWS Lambda proxy/enforcer for PrivateLink private channel traffic.",
  "main": "index.js",
  "type": "commonjs",
  "private": true,
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "test": "node --test",
    "build": "npm prune --production && rm -f ./function.zip && zip -r ./function.zip . -x './test/*' -x './src/test/*' -x '*.md' -x '*.env' -x '*.gitignore' -x './function.zip'",
    "test-build": "npm test && npm run-script build"
  },
  "dependencies": {},
  "devDependencies": {}
}
