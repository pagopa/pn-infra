{
  "name": "pn-critical-monitoring-notification",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test-build": "npm run-script build",
    "build": "npm prune --production && rm -f ./function.zip && zip -r ./function.zip . -x './src/test/*' -x '*.md' -x '*.env'  -x '*.gitignore'"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.1079.0",
    "@aws-sdk/client-s3": "^3.1079.0",
    "@aws-sdk/client-sns": "^3.1005.0",
    "eslint": "^8.23.1",
    "eslint-plugin-import": "^2.26.0"
  }
}
