{
  "name": "cdc-athena-view-cf-transform",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "nyc --reporter=html --reporter=text mocha './src/test/**/*.test.js' --recursive --timeout=3000 --exit ",
    "build": "npm --silent prune --production && rm -f ./function.zip && zip -q -r ./function.zip . -x './src/test/*' -x '*.md' -x '*.env'  -x '*.gitignore'",
    "test-build": "npm run-script test && npm run-script build"
  },
  "devDependencies": {
    "chai": "^4.5.0",
    "mocha": "^11.3.0",
    "mocha-lcov-reporter": "^1.3.0",
    "nyc": "^18.0.0"
  },
  "author": "",
  "license": "ISC"
}
