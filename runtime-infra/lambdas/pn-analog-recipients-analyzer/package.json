{
  "name": "pn-analog-recipients-analyzer",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test-build": "npm run-script build",
    "build": "npm prune --production && rm -f ./function.zip && zip -r ./function.zip . -x './src/test/*' -x '*.md' -x '*.env'  -x '*.gitignore'"
  },
  "dependencies": {
  },
  "devDependencies": {
    "eslint": "^8.23.1",
    "eslint-plugin-import": "^2.26.0"
  }
}
